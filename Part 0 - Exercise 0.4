1. Creating a New Note in the Traditional Web App
Sequence Diagram:

- User writes a note in the text field and clicks "Save".
- Browser sends a POST request to the Server with the note data.
- Server processes the request and updates the Database with the new note.
- Server sends back a new HTML page that includes the updated list of notes.
- Browser renders the new page, displaying the new note.
